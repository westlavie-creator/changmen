# OB

| 目录 | 用途 |
|------|------|
| `frontend/` | **浏览器采集** |
| `shared/` | HTTP / SaveBet 共用 |
| `@changmen/platform-node/ob/` | Node 库 + CLI |

```bat
cd changmen/packages/platform-node
npm run ob:view -- --match <match_id>
```