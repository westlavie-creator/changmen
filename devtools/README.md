# devtools

与生产运行时无关的开发/探针工具。

| 目录 | npm 包 | 用途 |
|------|--------|------|
| `platform-probes/` | `@changmen/platform-probes` | 直连各平台 HTTP/MQTT 探针 CLI（可选） |

主链路采集见 `client/platform-adapter/*/frontend`。OB 锁盘观察见 `client/platform-adapter/ob/shared/lock_decision.ts`。
