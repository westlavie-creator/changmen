# devtools

与生产运行时无关的开发/探针工具。

| 目录 | npm 包 | 用途 |
|------|--------|------|
| `platform-probes/` | `@changmen/platform-probes` | 直连各平台 HTTP/MQTT 探针 CLI（可选） |
| 棒球独立 Vite 站归档 | — | 已下线；归档副本已从仓库移除 |

主链路采集见 `packages/venue-adapter/{platform}/`（`collect.ts` / `bet.ts`）。OB 锁盘观察见 `packages/venue-adapter/ob/shared/lock_decision.ts`。
