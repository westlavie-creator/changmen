{
  "name": "@changmen/match-composer",
  "type": "module",
  "version": "1.0.0",
  "description": "从零合场+主客投影：不调用 match-engine merge/finalize/reconcile",
  "scripts": {
    "start": "node loop.js",
    "once": "node scripts/once.mjs",
    "diff": "node scripts/diff.mjs",
    "ui": "node ui/server.js",
    "test": "node scripts/check-no-merge-import.mjs && vitest run",
    "check:no-merge-import": "node scripts/check-no-merge-import.mjs"
  },
  "dependencies": {
    "@changmen/db": "*",
    "@changmen/match-engine": "*",
    "@changmen/shared": "*",
    "@changmen/storage": "*",
    "@changmen/team-resolver": "*",
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  }
}
