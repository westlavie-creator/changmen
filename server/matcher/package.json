{
  "name": "@changmen/matcher",
  "version": "1.0.0",
  "type": "module",
  "description": "赛事匹配管理 — 独立 Web 面板 + 后台匹配调度",
  "scripts": {
    "start": "node ui/server.js",
    "matcher": "node scripts/start-db.mjs",
    "matcher:rds": "node scripts/start-rds.mjs",
    "test": "npm run test --prefix ../match-engine && node --test ui/matcher_auth.test.mjs lib/heartbeat.test.mjs"
  },
  "dependencies": {
    "@changmen/db": "*",
    "@changmen/storage": "*",
    "@changmen/match-engine": "*",
    "@changmen/shared": "*",
    "@changmen/team-resolver": "*",
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  }
}
