{
  "name": "@changmen/matcher",
  "type": "module",
  "version": "1.0.0",
  "description": "赛事匹配管理 — 人工 Web 面板 + match-engine 库",
  "scripts": {
    "start": "node ui/server.js",
    "test": "npm run test --prefix ../match-engine && vitest run"
  },
  "dependencies": {
    "@changmen/db": "*",
    "@changmen/match-engine": "*",
    "@changmen/shared": "*",
    "@changmen/storage": "*",
    "@changmen/team-resolver": "*",
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  }
}
