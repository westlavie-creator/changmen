{
  "name": "@changmen/matcher",
  "type": "module",
  "version": "1.0.0",
  "description": "赛事匹配管理 — 独立 Web 面板 + 后台匹配调度",
  "scripts": {
    "start": "node ui/server.js",
    "matcher": "node scripts/start-db.mjs",
    "matcher:rds": "node scripts/start-rds.mjs",
    "test": "npm run test --prefix ../match-engine && vitest run"
  },
  "dependencies": {
    "@changmen/db": "*",
    "@changmen/match-engine": "*",
    "@changmen/shared": "*",
    "@changmen/storage": "*",
    "@changmen/team-resolver": "*",
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  }
}
