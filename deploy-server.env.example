REM Copy to deploy-server.env and edit if needed
set "DEPLOY_USER=root"
set "DEPLOY_HOST=47.82.100.166"
REM Git clone root on VPS (parent of changmen/, or changmen/ itself if you cloned into that folder)
set "DEPLOY_REPO=/root/gamebet"
