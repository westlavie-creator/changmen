REM Copy to deploy-server.env and edit if needed
set "DEPLOY_USER=root"
set "DEPLOY_HOST=47.82.100.166"
set "DEPLOY_REPO=/root/gamebet"

REM Recommended: build frontend on your PC, upload dist (skip slow VPS vite build)
set "DEPLOY_LOCAL_BUILD=1"

REM Set to 1 to force full npm install + app:build on VPS (ignore git diff)
REM set "DEPLOY_FULL=1"
